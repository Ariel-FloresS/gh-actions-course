name: 18.3 - Reusable Workflows

on:
    workflow_dispatch:



jobs:

    deploy-job:

        uses: ./.github/workflows/18-1-reusable-workflow.yaml
        with:
            target-directory: dummy-dir

    e2e-tests:
        # uses: <Owner>/<repository>/.github/workflows/<workflow_name>@<version/branch/tag/sha>
        uses: Ariel-FloresS/github-actions-ourse-example-e2e/.github/workflows/e2e.yaml@main


    

